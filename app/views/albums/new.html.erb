<div class="header">
  <%= image_tag 'create-album-header.png', :class => 'title' %>
  <a href="#" onclick="$open_overlay.close(); return false;"><%= image_tag 'event-form-close.png' %></a>
</div>

<%= form_for([@trip, @album], :html => {:class => 'album_form'}) do |f| %>
<%= render 'form', :f => f %>
<% end %>
