<!DOCTYPE html>
<html>
<head>
  <title>Plan it. Live it. Share it.</title>
  <%= stylesheet_link_tag :all %>
  <style>
	html, body{
		background: #4C859E;
	}
	#content {
	  height: 520px;
	}
	#content .text {
	  font-size: 120%;
	  color: #2c5f78;
	  width: 550px;
	  line-height: 35px;
	  position: absolute;
	  right: 50px;
	  top: 40px;
	  z-index: 1000;
	}

  #content .button {
    position: absolute;
    top: 200px;
    right: 70px;
  }

  #content .image {
    position: absolute;
    top: 50px;
    left: 3px;
  }

	#content .banner {
	  position: absolute;
	  top: 300px;
	  left: 472px;
	}
  </style>
</head>
<body>
  <div id="header">
   <div id="user_nav">
     <% if user_signed_in? %>
       <%= %w{Hello Hi Hola Bonjour Privet Guten\ Tag Hallo Ciao}.sample %>, <%= current_user.username %>. 
    		<%= link_to 'Your Trips', trips_path %>
    		<%= link_to 'Create Trip', new_trip_path %>
       <%= link_to "Sign out", destroy_user_session_path %>
     <% else %>
       <%= link_to "Sign up", new_user_registration_path %> or <%= link_to "sign in", new_user_session_path %>
     <% end %>
   </div>

   <div id="logo"><%= link_to image_tag('logo.png'), '/' %></div>
  </div>
   
  <div id="content">
    <div class="text">Plan your vacation and then share all of your memories with friends and family. We can help with the planning and sharing, the living part is up to you.</div>
    <div class="button"><%= link_to image_tag('planyourtrip.png'), new_user_session_path %></div>
    <div class="image"><%= image_tag('cluster.png') %></div>
    <div class="banner"><%= image_tag('banner.png') %></div>
  </div>
</body>
</html>